# ============================================================================
# AADE Airbnb Automation - Environment Configuration
# ============================================================================
# Copy this file to .env and fill in your values:
#   cp .env.example .env
# ============================================================================

# ===== REQUIRED =====

# OpenAI API Key (Stagehand uses this for AI-powered webpage understanding)
# Get one at: https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-your-openai-api-key-here

# ===== BROWSER MODE =====

# Where to run the browser: LOCAL (your machine) or BROWSERBASE (cloud)
# Default: LOCAL
STAGEHAND_ENV=LOCAL

# Browserbase credentials (only needed if STAGEHAND_ENV=BROWSERBASE)
# Get these at: https://browserbase.com
BROWSERBASE_API_KEY=your_browserbase_api_key
BROWSERBASE_PROJECT_ID=your_browserbase_project_id

# ===== SAFETY MODE =====

# DRY_RUN=true  → Navigate forms but DON'T actually submit (safe for testing)
# DRY_RUN=false → Actually submit declarations to AADE (production mode)
DRY_RUN=true

# ===== OPTIONAL: Real Airbnb Extraction =====

# Only needed if using real Airbnb (make run-ingest)
# Note: Airbnb has strong bot detection - mock mode recommended for development
AIRBNB_EMAIL=your_email@example.com
AIRBNB_PASSWORD=your_password

# ===== OPTIONAL: AADE Contact Info =====

# These are auto-filled if AADE asks for contact info after login
# Defaults are test values - replace with real ones for production
AADE_PHONE=2101234567
AADE_MOBILE=6971234567
AADE_EMAIL=your_email@example.com
